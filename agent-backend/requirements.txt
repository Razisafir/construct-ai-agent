# Construct AI Agent — Python Backend Dependencies
# =================================================

# Vector database (persistent SQLite-backed storage)
chromadb>=0.5.0

# Sentence embeddings (local, no API key needed)
sentence-transformers>=3.0.0

# Web framework
fastapi>=0.115.0
uvicorn>=0.32.0

# Data validation
pydantic>=2.9.0

# Configuration
python-dotenv>=1.0.0

# ---------------------------------------------------------------------------
# LLM Providers
# ---------------------------------------------------------------------------

# OpenAI (GPT-4o, GPT-4o-mini)
openai>=1.55.0

# Anthropic (Claude 3.5 Sonnet, Claude 3 Opus)
anthropic>=0.40.0

# Google (Gemini 1.5 Pro)
google-generativeai>=0.8.0

# Async HTTP for Ollama + Google REST API + Tauri notifications
aiohttp>=3.11.0

# ---------------------------------------------------------------------------
# Code Analysis
# ---------------------------------------------------------------------------

# AST -> source code conversion for Python refactoring
astor>=0.8.1

# ---------------------------------------------------------------------------
# Autonomous Agent (Background Worker + Checkpoint + Safety)
# ---------------------------------------------------------------------------

# System resource monitoring (CPU, memory, disk)
psutil>=6.0.0

# Task scheduling
schedule>=1.2.0

# ---------------------------------------------------------------------------
# Document Conversion
# ---------------------------------------------------------------------------

# Microsoft MarkItDown — convert 20+ document formats to Markdown
markitdown[all]>=0.1.0